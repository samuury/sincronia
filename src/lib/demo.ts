// Modo demo desativado — todo conteúdo agora vem da API.
// Stubs mantidos apenas para não quebrar imports existentes.

export function isDemo(): boolean {
  return false;
}

export function enableDemo() {
  /* no-op */
}

export function disableDemo() {
  /* no-op */
}
